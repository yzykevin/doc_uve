# Libraries

All Information can be found here: `verif/lib/info`

## -svlib

Enable `svlib` so related functions/tasks can be used. Note: in regression, this should be enabled if use svlib

## -syoscb

Enable `syoscb` so related func/task can be used. Note: in regression, this should be enabled if use syoscb
